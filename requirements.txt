# fbxsdkpy==2020.1.post2 — OPTIONAL, FBX export only.
# Autodesk download URLs are broken; install manually if needed:
#   pip install fbxsdkpy --extra-index-url https://gitlab.inria.fr/api/v4/projects/18692/packages/pypi/simple
# The code gracefully falls back to GLB export when fbx is unavailable.

torchdiffeq>=0.2.5
transforms3d>=0.4.2
omegaconf>=2.3.0
accelerate>=0.30.0
huggingface_hub>=0.30.0
bitsandbytes>=0.42.0